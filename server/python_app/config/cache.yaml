# =============================================================================
#   Cache 配置
# =============================================================================
cache:
  prompt:
    type: memory
    ttl: 3600
  embedding:
    type: memory
    ttl: 86400
  result:
    type: memory
    ttl: 600
